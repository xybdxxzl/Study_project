# STL容器（我碰到过的）

### multiset（多重集）

会排序，会存下多个相同值的集合，开始的地方是begin(),结束的地方是end()

#### 1，lower_bound()

查找小于等于的某个值的第一个数

打印需要解引用*

#### 2，upper_bound()

查找大于等于的某个值的第一数

#### 3，count()

统计某个数字出现了几次





### vector(队列)

#### 1，.back()访问最后一个元素

#### 2， .empty()是否为空

如果为空那么返回一个真值，如果不为空返回一个假值。
